{
  "name": "mozvr-gazebo",
  "version": "0.1.9",
  "homepage": "https://github.com/MozVR/vr-markup",
  "license": "MIT",
  "main": "src/vr-markup.js",
  "dependencies": {
    "document-register-element": "^0.4.5",
    "three-dev": "^0.72.0",
    "tween.js": "^15.0.0"
  },
  "devDependencies": {
    "autoprefixer-loader": "^3.1.0",
    "css-loader": "^0.18.0",
    "gh-pages": "^0.4.0",
    "husky": "^0.10.1",
    "karma": "^0.13.9",
    "karma-chai-shallow-deep-equal": "0.0.4",
    "karma-firefox-launcher": "^0.1.4",
    "karma-mocha": "^0.2.0",
    "karma-mocha-reporter": "^1.1.0",
    "karma-sinon-chai": "^1.1.0",
    "karma-webpack": "^1.7.0",
    "mozilla-download": "^1.0.5",
    "replace": "^0.3.0",
    "semistandard": "^7.0.2",
    "shader-loader": "^1.1.3",
    "snazzy": "^2.0.1",
    "style-loader": "^0.12.4",
    "uglifyjs": "^2.4.10",
    "webpack": "^1.12.2",
    "webpack-dev-server": "^1.11.0"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "webpack-dev-server --content-base src --progress --watch --inline --hot",
    "build": "NODE_ENV=production webpack --progress",
    "build:dev": "webpack --progress --hot",
    "dist": "rm -rf dist/* && npm run build && npm run build:dev && cp build/* dist && git commit -am 'Bump dist'",
    "replacepaths": "replace '../build/vr-markup.js' '../dist/vr-markup.min.js' gh-pages/ -r && replace '../style/vr-markup.css' '../dist/vr-markup.min.css' gh-pages/ -r",
    "gh-pages": "npm run ghpages",
    "preghpages": "mkdir -p gh-pages && cp -r {components,dist,examples,lib} gh-pages/. && npm run replacepaths",
    "ghpages": "gh-pages -d gh-pages && rm -rf gh-pages",
    "release": "npm version patch --minor && npm login && npm publish",
    "test": "karma start ./test/karma.conf.js",
    "test:ci": "karma start ./test/karma.conf.js --single-run",
    "lint": "semistandard -v $(git ls-files '*.js') | snazzy",
    "precommit": "npm run lint"
  },
  "repository": "MozVR/vr-markup",
  "bugs": {
    "url": "https://github.com/MozVR/vr-markup/issues"
  },
  "semistandard": {
    "ignore": [
      "dist/**",
      "examples/js/**",
      "lib/cursor3D.js",
      "lib/vendor/**",
      "src/shaders/**"
    ]
  }
}
